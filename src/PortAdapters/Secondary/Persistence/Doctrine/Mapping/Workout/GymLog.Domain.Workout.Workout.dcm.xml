<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="GymLog\Domain\Workout\Workout" table="workout">

        <indexes>
            <index name="workout_user_started_at_index" columns="user_id,started_at"/>
            <!-- Под «последняя завершённая тренировка пользователя»: повтор тренировки и показ прошлого раза. -->
            <index name="workout_user_finished_at_index" columns="user_id,finished_at">
                <options>
                    <option name="where">(finished_at IS NOT NULL)</option>
                </options>
            </index>
        </indexes>

        <unique-constraints>
            <!--
                Инвариант «одна активная тренировка на пользователя».
                Частичный уникальный индекс: ограничение действует только на строки с finished_at IS NULL.
            -->
            <unique-constraint name="workout_active_per_user_uniq_index" columns="user_id">
                <options>
                    <!-- Форма предиката — как его нормализует Postgres, см. комментарий в маппинге Exercise. -->
                    <option name="where">(finished_at IS NULL)</option>
                </options>
            </unique-constraint>
        </unique-constraints>

        <id name="id" type="string" length="64" column="id">
            <generator strategy="NONE"/>
        </id>

        <field name="startedAt" type="datetimetz_immutable" column="started_at" nullable="false"/>
        <field name="finishedAt" type="datetimetz_immutable" column="finished_at" nullable="true"/>
        <field name="note" type="text" column="note" nullable="true"/>

        <many-to-one field="user" target-entity="GymLog\Domain\User\User" fetch="LAZY">
            <join-column name="user_id" referenced-column-name="id" nullable="false" on-delete="CASCADE"/>
        </many-to-one>

        <one-to-many field="exercises" target-entity="GymLog\Domain\Workout\WorkoutExercise" mapped-by="workout" orphan-removal="true">
            <cascade>
                <cascade-persist/>
                <cascade-remove/>
            </cascade>
            <order-by>
                <order-by-field name="position" direction="ASC"/>
            </order-by>
        </one-to-many>

    </entity>

</doctrine-mapping>